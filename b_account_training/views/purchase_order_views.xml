<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <!-- purchase.order inherit form view -->
    <record id="purchase_order_view_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.view.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='reminder']" position="after">
                <field name="is_training_purchase"
                       readonly="state not in ('draft', 'sent')"
                       groups="b_account_training.user_account_training"/>
            </xpath>
        </field>
    </record>
    <!-- purchase.order inherit search view -->
    <record id="purchase_order_view_search_inherit" model="ir.ui.view">
        <field name="name">purchase.order.view.search.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.view_purchase_order_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='to_approve']" position="after">
                <separator/>
                <filter string="Training Orders" name="training" domain="[('is_training_purchase','=',True)]"
                    groups="b_account_training.user_account_training"/>
            </xpath>
        </field>
    </record>

    <record id="view_purchase_order_search_inherit" model="ir.ui.view">
        <field name="name">report.purchase.order.search.inherit</field>
        <field name="model">purchase.report</field>
        <field name="inherit_id" ref="purchase.view_purchase_order_search"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='product_id']" position="after">
                <separator/>
                <filter string="Training Orders" name="training" domain="[('is_training_purchase','=',True)]"
                    groups="b_account_training.user_account_training"/>
            </xpath>
        </field>
    </record>
</odoo>